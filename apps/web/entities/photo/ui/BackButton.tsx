'use client';

import { Button } from '@repo/ui/button';

export default function BackButton() {
  return (
    <Button size='default' className='w-full sm:max-w-[154px]'>
      이전
    </Button>
  );
}
